<h1 class="text-bold text-xl text-white neon-text">Product Detail</h1>
{{#with product}}
  <section class="flex">
    {{!-- <img src={{thumbnail}} alt="product image" /> --}}
    <div class="bg-gray-800 shadow-lg rounded-lg p-8 max-w-sm w-full">
      <p class="text-white">Name: {{title}}</p>
      <p class="text-white">Description: {{description}} </p>
      <p class="text-white">Category: {{category}} </p>
      <p class="text-white">Stock: {{stock}} </p>
      <p class="text-white"> ${{price}}</p>
      <button
        onclick="addProductToCart('{{id}}');"
        class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
        type="button"
      >Add to Cart</button>
    </div>
  </section>
{{/with}}
<script src="../../static/index.js"></script>